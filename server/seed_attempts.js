const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.eC_User.findUnique({ where: { email: 'test@example.com' } }); if (!user) return console.log('User not found'); await prisma.eC_TestAttempt.create({ data: { userId: user.id, examId: 'js-exam-1', score: 80, status: 'PASSED', attemptDate: new Date(), completedAt: new Date() } }); await prisma.eC_Certification.create({ data: { userId: user.id, examId: 'js-exam-1', certificateUrl: 'https://example.com/cert/123' } }); console.log('Attempt and certification added!'); } main().catch(console.error).finally(() => prisma.$disconnect());
