const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const hashedPassword = await bcrypt.hash('admin123', 10); await prisma.eC_User.upsert({ where: { email: 'admin@skillcertify.com' }, update: { password: hashedPassword, role: 'ADMIN' }, create: { email: 'admin@skillcertify.com', name: 'Admin', password: hashedPassword, role: 'ADMIN' } }); console.log('Admin user created/updated!'); } main().catch(console.error).finally(() => prisma.$disconnect());
